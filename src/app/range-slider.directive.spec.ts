import { RangeSliderDirective } from './range-slider.directive';

describe('RangeSliderDirective', () => {
  it('should create an instance', () => {
    const directive = new RangeSliderDirective();
    expect(directive).toBeTruthy();
  });
});
